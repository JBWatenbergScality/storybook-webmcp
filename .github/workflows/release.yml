name: Publish to GitHub Packages

on:
  release:
    types: [published]

permissions:
  contents: read
  packages: write

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          registry-url: 'https://npm.pkg.github.com'
          scope: '@jbwatenbergscality'

      # `npm install` (not `npm ci`) because the lockfile is platform-locked
      # to whichever OS first ran install; native optional deps differ on Linux.
      - run: npm install --no-audit --no-fund

      - run: npm test
      - run: npx playwright install --with-deps chromium
      - run: npm --prefix examples/fixture-sb install --no-audit --no-fund
      - run: npm --prefix examples/fixture-sb run build-storybook
      - run: npm run test:e2e

      - run: npm run build

      - name: Set package version from release tag
        run: npm version --no-git-tag-version "${GITHUB_REF_NAME#v}"

      - run: npm publish
        env:
          NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
